name: On Release, Build release zip

on:
  release:
    types: [published]

jobs:
  build:
    name: Build release zip
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install production dependencies
        run: composer install --no-dev --optimize-autoloader

      - name: Archive Release
        uses: thedoctor0/zip-release@b57d897cb5d60cb78b51a507f63fa184cfe35554 #0.7.6
        with:
          type: 'zip'
          filename: 'vmfa-search.zip'
          exclusions: >-
            *.git* /*.github/* /*.vscode/* /*node_modules/* /*tests/* /*docs/*
            composer.json composer.lock package.json package-lock.json
            phpunit.xml phpunit.xml.dist phpcs.xml vitest.config.js i18n-map.json
            .editorconfig .phpunit.cache .gitignore .gitattributes .distignore
            *.log CHANGELOG.md README.md CONTEXT.md
            !readme.txt !assets/* !languages/* !src/php/* !vendor/*

      - name: Verify Archive Contents
        run: |
          echo "Listing vmfa-search.zip entries:"
          unzip -l vmfa-search.zip || { echo "Failed to list zip contents"; exit 1; }
          echo "Total files: $(unzip -l vmfa-search.zip | awk 'END{print $2}')"

          # Verify Action Scheduler is included (required dependency)
          if unzip -l vmfa-search.zip | grep -q "vendor/woocommerce/action-scheduler/action-scheduler.php"; then
            echo "✓ Action Scheduler is included (vendor/)"
          elif unzip -l vmfa-search.zip | grep -q "woocommerce/action-scheduler/action-scheduler.php"; then
            echo "✓ Action Scheduler is included (woocommerce/)"
          else
            echo "✗ ERROR: Action Scheduler is missing from the release!"
            exit 1
          fi

      - name: Release
        uses: softprops/action-gh-release@c95fe1489396fe8a9eb87c0abf8aa5b2ef267fda #v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: vmfa-search.zip
          tag_name: ${{ github.event.release.tag_name }}
